import React from "react";
import Layout from "@/src/components/Layout";
const CheckoutIndex = () => {
    return (
        <Layout>
            <div>Checkout</div>
        </Layout>
    );
};

export default CheckoutIndex;
